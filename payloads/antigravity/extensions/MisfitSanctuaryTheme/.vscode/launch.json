{
  // Standard VS Code extension debugging config
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run MisfitSanctuary Theme",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
